{% extends 'base.html.twig' %}

{% block title %}Edit Options{% endblock %}

{% block body %}
    <h4 class="widget-header">Edit Options</h4>
<div class="col-lg-6 col-12 mx-auto">
    {{ include('options/_form.html.twig', {'button_label': 'Update'}) }}
</div>
    <a href="{{ path('options_index') }}">back to list</a>

    {{ include('options/_delete_form.html.twig') }}
{% endblock %}
