{% extends 'base.html.twig' %}

{% block title %}ReservationHotel{% endblock %}

{% block body %}
    <h1>ReservationHotel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation_hotel.id }}</td>
            </tr>
            <tr>
                <th>Debut</th>
                <td>{{ reservation_hotel.debut ? reservation_hotel.debut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Fin</th>
                <td>{{ reservation_hotel.fin ? reservation_hotel.fin|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reservation_hotel_index') }}">back to list</a>

    <a href="{{ path('reservation_hotel_edit', {'id': reservation_hotel.id}) }}">edit</a>

    {{ include('reservation_hotel/_delete_form.html.twig') }}
{% endblock %}
