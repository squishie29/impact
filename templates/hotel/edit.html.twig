{% extends 'base.html.twig' %}

{% block title %}Edit Hotel{% endblock %}

{% block body %}
    <h4 class="widget-header">Edit Hotel</h4>
    <div class="col-lg-6 col-12 mx-auto">
    {{ include('hotel/_form.html.twig', {'button_label': 'Update'}) }}
    </div>
    <a  class="btn-outline-primary" href="{{ path('hotel_index') }}">back to list</a>
    {{ include('hotel/_delete_form.html.twig') }}
{% endblock %}
