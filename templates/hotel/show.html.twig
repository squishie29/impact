{% extends 'base.html.twig' %}

{% block title %}Hotel{% endblock %}

{% block body %}
    <h4>Hotel</h4>
<div class="table-responsive">
    <table class="table table-bordered table-hover table-striped mb-4">

        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ hotel.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ hotel.name }}</td>
            </tr>
            <tr>
                <th>Stars</th>
                <td>{{ hotel.stars }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ hotel.photo }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ hotel.description }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ hotel.adress }}</td>
            </tr>
        </tbody>
    </table>
</div>

    <a class="btn-outline-primary"  href="{{ path('hotel_index') }}">back to list</a>

    <a class="btn-outline-primary"  href="{{ path('hotel_edit', {'id': hotel.id}) }}">edit</a>

    {{ include('hotel/_delete_form.html.twig') }}
{% endblock %}
